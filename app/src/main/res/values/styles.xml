<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="PreferenceFixTheme.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="SensorValuesTV">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:paddingBottom">4dp</item>
        <item name="android:paddingLeft">2dp</item>
        <item name="android:text">Values</item>
        <item name="android:textSize">11sp</item>
    </style>

    <style name="SensorLabelTV">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">12sp</item>
        <item name="android:textColor">#000000</item>
    </style>
    <style name="PreferencesTheme" parent="PreferenceFixTheme.DayNight.DarkActionBar">
        <item name="android:windowBackground">@color/colorPrimary</item>
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>
        <item name="android:textColor">@color/colorText</item>
        <item name="android:textColorSecondary">@color/colorTextSecond</item>
        <item name="android:textColorPrimary">@color/colorText</item>
        <item name="android:dialogTheme">@style/SettingsDialog</item>
        <item name="android:alertDialogTheme">@style/SettingsDialog</item>
    </style>
    <style name="SettingsDialog" parent="Theme.AppCompat.DayNight.Dialog">
        <item name="android:textColor">@color/colorTextSecond</item>
        <item name="android:textColorAlertDialogListItem">@color/colorTextSecond</item>
        <item name="android:background">@color/colorPrimaryDark</item>
    </style>
</resources>
